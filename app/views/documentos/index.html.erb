<h1>Listing documentos</h1>

<table>
  <thead>
    <tr>
      <th>Num expediente</th>
      <th>Asunto</th>
      <th>Folio</th>
      <th>Ubic archivo</th>
      <th>Rpta</th>
      <th>Firma</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @documentos.each do |documento| %>
      <tr>
        <td><%= documento.num_expediente %></td>
        <td><%= documento.asunto %></td>
        <td><%= documento.folio %></td>
        <td><%= documento.ubic_archivo %></td>
        <td><%= documento.rpta %></td>
        <td><%= documento.firma %></td>
        <td><%= link_to 'Show', documento %></td>
        <td><%= link_to 'Edit', edit_documento_path(documento) %></td>
        <td><%= link_to 'Destroy', documento, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Documento', new_documento_path %>
